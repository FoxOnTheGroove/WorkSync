class Capture:
    """Capture the active viewport and export it as an image file."""

    @staticmethod
    def get_window():
        # TODO: return the target ui.Window
        return None

    @staticmethod
    def capture_to_file(file_path):
        # TODO: capture the current Kit app viewport and write it to file_path
        print(f"[capt] capture_to_file -> {file_path}")
        return False
